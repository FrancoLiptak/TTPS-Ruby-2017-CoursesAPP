<%# Rails flash messages styled for Bootstrap 4.0 %>
<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
  <div class="alertContainer container col-md-6">
    <div class="alert alert-dismissable center-block fade show alert-<%= name.to_s == 'notice' ? 'success' : 'danger' %>" role="alert">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <center><%= content_tag :div, msg, :id => "flash_#{name}" %></center>
    </div>
  </div>
  <% end %>
<% end %>