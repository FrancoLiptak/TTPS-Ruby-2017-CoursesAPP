<nav class="navbar navbar-inverse" style="background-color: #424242;">
    <div class="container">
        <a href="<%= courses_path %>"><%= image_tag("icon.png", width: "60", height: "60") %></a>
        <a style="color: #FAFAFA;" >Ruby on Rails - Courses</a>
        <ul class="nav nav-pills">
            <li class="nav-item">
                <% if user_signed_in? %>
                    <li class="nav-item">
                        <%= link_to 'Log out', destroy_user_session_path, method: :delete, :class => "btn btn-warning" %>
                    </li>
                <% else %>
                    <li class="nav-item">
                        <%= link_to 'Log in', new_user_session_path, :class => "btn btn-success" %>
                    </li>
                <% end %>
            </li>
        </ul>
    </div>
</nav>